language: python
python:
  - '3.9'
env:
  - 'PYTHONPATH=$PYTHONPATH:$TRAVIS_BUILD_DIR/py'
install:
  - cd py && pip install --upgrade -r requirements/requirements.lock
jobs:
  include:
    - stage: Lint
      name: Pylint
      script: pylint qgraph/qgraph.py
    - stage: Typing
      name: Mypy
      script: mypy qgraph/qgraph.py
    - stage: Test
      name: Pytest
      script: pytest
